# SYNTHETIC FIXTURE — fake Kubernetes Secret for testing runtime-vault.
# All base64 values decode to obviously-fake demo strings, e.g.
# ZGVtby11c2Vy == base64("demo-user"). Never put real secrets here.
apiVersion: v1
kind: Secret
metadata:
  name: gateway-credentials
  namespace: staging
type: Opaque
data:
  # ZGVtby11c2Vy == "demo-user"
  username: ZGVtby11c2Vy
  # ZGVtby1wYXNzd29yZA== == "demo-password"
  password: ZGVtby1wYXNzd29yZA==
  # ZGVtby1hcGktdG9rZW4= == "demo-api-token"
  api-token: ZGVtby1hcGktdG9rZW4=
stringData:
  # stringData is plaintext by design; still obviously fake.
  connection-note: "demo-only, not a real credential"
